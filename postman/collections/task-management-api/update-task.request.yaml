$kind: http-request
method: PATCH
url: '{{base_url}}/tasks/{{task_id}}'
order: 6000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{auth_token}}'
body:
  type: json
  content: |-
    {
      "status": "done"
    }
